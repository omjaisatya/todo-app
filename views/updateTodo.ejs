<%- include("partials/header") %>
    <div class="table-container container rounded rounded-5 border-black">
      <h2 class="todo-title">Update todo</h2>

      <div>
        <a class="btn btn-primary" href="/">Back</a>

        <form action="/update-todo/<%= todo._id %>" method="post">
          <div class="update-todo mb-3">
            <div class="form-group">
              <label for="title" class="text-light">Title</label>
              <input
                type="text"
                name="title"
                value="<%= todo.title %>"
                placeholder="Type todo here"
              />
            </div>

            <div class="form-group mb-3">
              <label for="desc" class="text-light">Description</label>
              <input
                type="text"
                name="desc"
                value="<%= todo.desc %>"
                placeholder="Type description here"
              />
            </div>

            <div class="form-group">
              <button type="submit" class="btn btn-success">Save</button>
            </div>
          </div>
        </form>
      </div>
    </div>
<%- include("partials/footer") %>